# 연구기록 유지

사용자는 TAPtoTEN의 주요 개발 전환을 논문 집필 참고자료로 보존한다.

- 주요 규칙·모드·흐름·디자인 변경 시 `docs/research/YYYY-MM-DD-topic/`에 문제/요청, 무엇을 왜 바꿨는지, 결과와 검증을 작성하고 연구 README 목록을 갱신한다.
- 작은 간격·폰트 조정은 해당 주요 변경에 묶는다. 매 사소한 수정마다 새 연구 항목을 만들지 않는다.
- 전후 화면은 390×844 CSS px / DPR 2의 **780×1688 PNG**로 통일한다. 같은 브라우저·시계·프로필 조건을 쓰고 차이는 설명한다.
- 과거 버전은 별도 임시 폴더에 git archive로 추출해서 실행한다. 현재 작업 소스를 과거 버전으로 교체하지 않는다. 당시 커밋, 실제 재현 촬영일, 캡처 조건, 원본 해시를 기록한다.
- 재현 실패는 이유를 적는다. 없는 과거 화면·설계 동기·실험 결과를 만들어 넣지 않는다. Git 메시지의 과거 테스트 주장은 이번 검증과 구분한다.
- 캡처/연구 스크립트는 게임에 import하지 않고 배포 런타임에 포함하지 않는다. 다른 게임 저장소는 읽기 참고만 한다.
- 기존 자료는 새 변화 때문에 덮어쓰지 않는다. 사용자가 해상도 등 기록 정책을 명시적으로 바꾸는 경우만 자료 규격을 갱신한다.
